**To Work on Today:**
- find out why on base (app) muscadine app has to be searxhed with .xyz not just putting in the name. Also why on the base app it loads, goes white. than have to go back in to work.
-delete possibly /transcaction page, and keep the back end for the vault pages. see if the two buttons with model or having a box on the right of the vault graph would be better.
-improve the token/stock pages 
**To work on another day:**

**Future (optional):**
-be multichain for viewing such as with stocks, vaults like robinhood chain. With the actual functions on settings be able to switch the chain. 
- Switch force exit to [morpho-org/bundles](https://github.com/morpho-org/bundles) `vaultExitBundlesV1ForceWithdrawVaultV2` when Base addresses exist.
- Fold force+ETH unwrap into one Bundler3 flow if Morpho adds Base exit bundles.
- Smart wallet (AA) deposit issue when USDC is used for gas — investigate before changing tx code.
- ABI vs Morpho SDK for v2 txs: keep direct ABIs as default until wagmi 2.x constraint clears.
