**To Work on Today:**
- find out why on base (app) muscadine app has to be searched app.muscadine.xyz not just putting in the name like the other miniapps. Also, this happens sometimes, but not anytime recently, why on the base app when i go into the app, it loads and is normal for a few seconds, than it goes blank. I have to click out of the app than back in for it to work normally after and get off blank screen. Research this and see what is causing it for just the miniapp. Is it using the base connection instead of rainbow which it should? Before you do anything with this section, tell me.

-delete the /transcact page, and keep but reorganize back end for the (/transcact) and vault pages to have the transcactions if it is needed to be more optimized with /transcact gone. see if the two buttons with model or having a box on the right of the vault graph would be better for ui (graph is lowk too big). I do though like the two buttons on the bottom for mobile view. Before you do anything with this section, tell me.

-Per ignas, having a weekly, monthly, and yearly earning breakdown from the vaults would be awesome. You could do past weekly, monthly, yearly and all time earning rewards. If a wallet never had a position above a certain time it can be hidden. See where it would fit best witht the ui. Wondering also how we can do future rewards too based off currenty net apy. Before you do anything with this section, tell me.

-improve the token/stock pages. I want to abstract away the crypto a little bit on dashboard. USDC can be changed to USD, with a USD symbol, but keep the same page, including any other stablecoins. Think about how to abstract it away. On the dashboard, should we have cash (usd) as its own section, or keep it USDC? Should stocks and crypto(tokens) have their own sections, and should vaults be on the dashbpard since technaclly this is a morpho vaults app. Before you do anything with this section, tell me.

**To work on another day:**

**Future (optional):**
-be multichain for viewing such as with stocks, vaults like robinhood chain. With the actual functions on settings be able to switch the chain. 
- Switch force exit to [morpho-org/bundles](https://github.com/morpho-org/bundles) `vaultExitBundlesV1ForceWithdrawVaultV2` when Base addresses exist.
- Fold force+ETH unwrap into one Bundler3 flow if Morpho adds Base exit bundles.
- Smart wallet (AA) deposit issue when USDC is used for gas — investigate before changing tx code.
- ABI vs Morpho SDK for v2 txs?
